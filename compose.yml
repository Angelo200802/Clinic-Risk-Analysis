services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    #environment:
    #  - APP_ENV=production
    #  - APP_DEBUG=false
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
